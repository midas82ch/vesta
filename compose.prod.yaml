x-logging: &default-logging
  driver: json-file
  options:
    max-size: "10m"
    max-file: "3"

services:
  proxy:
    image: caddy:2-alpine
    restart: unless-stopped
    environment:
      VESTA_SITE_ADDRESS: ${VESTA_SITE_ADDRESS:-http://localhost}
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - ./infra/caddy/Caddyfile:/etc/caddy/Caddyfile:ro
      - caddy-data:/data
      - caddy-config:/config
    networks:
      - frontend
    depends_on:
      web:
        condition: service_healthy
    security_opt:
      - no-new-privileges:true
    logging: *default-logging

  web:
    build:
      context: .
      dockerfile: apps/web/Dockerfile
    restart: unless-stopped
    environment:
      HOSTNAME: 0.0.0.0
      VESTA_API_URL: http://api:8000
    expose:
      - "3000"
    networks:
      - frontend
      - backend
    depends_on:
      api:
        condition: service_healthy
    healthcheck:
      test:
        [
          "CMD",
          "node",
          "-e",
          "fetch('http://127.0.0.1:3000').then(r=>{if(!r.ok)process.exit(1)}).catch(()=>process.exit(1))",
        ]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s
    security_opt:
      - no-new-privileges:true
    logging: *default-logging

  api:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    restart: unless-stopped
    environment:
      DATABASE_URL_FILE: /run/secrets/database-url
      OPENAI_API_KEY_FILE: /run/secrets/openai-api-key
      VESTA_AI_ENABLED: ${VESTA_AI_ENABLED:-false}
      VESTA_AI_PROVIDER: ${VESTA_AI_PROVIDER:-openai}
      VESTA_OPENAI_MODEL: ${VESTA_OPENAI_MODEL:-gpt-5.4-mini-2026-03-17}
      VESTA_ENV: production
    secrets:
      - database-url
      - openai-api-key
    expose:
      - "8000"
    networks:
      - backend
      - egress
    depends_on:
      migrate:
        condition: service_completed_successfully
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/ready')",
        ]
      interval: 15s
      timeout: 5s
      retries: 5
      start_period: 20s
    security_opt:
      - no-new-privileges:true
    logging: *default-logging

  migrate:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    command:
      [
        "alembic",
        "-c",
        "/app/apps/api/alembic.ini",
        "upgrade",
        "head",
      ]
    environment:
      DATABASE_URL_FILE: /run/secrets/database-admin-url
    secrets:
      - database-admin-url
    networks:
      - egress
    security_opt:
      - no-new-privileges:true
    logging: *default-logging

  ingest:
    build:
      context: .
      dockerfile: apps/api/Dockerfile
    command: ["python", "-m", "vesta_api.cli.ingest_offers"]
    environment:
      DATABASE_URL_FILE: /run/secrets/database-ingest-url
      OFFER_SOURCE_CATALOG_PATH: /app/data/sources/bern_offers.json
      VESTA_ENV: production
    secrets:
      - database-ingest-url
    networks:
      - egress
    profiles:
      - jobs
    security_opt:
      - no-new-privileges:true
    logging: *default-logging

networks:
  frontend:
  backend:
    internal: true
  egress:

volumes:
  caddy-data:
  caddy-config:

secrets:
  database-admin-url:
    file: ${DATABASE_ADMIN_URL_FILE:-./secrets/database-admin-url}
  database-url:
    file: ${DATABASE_URL_FILE:-./secrets/database-url}
  database-ingest-url:
    file: ${DATABASE_INGEST_URL_FILE:-./secrets/database-ingest-url}
  openai-api-key:
    file: ${OPENAI_API_KEY_FILE:-./secrets/openai-api-key}
